import { SanitizationInterceptor } from './sanitization.interceptor';

describe('SanitizationInterceptor', () => {
  it('should be defined', () => {
    expect(new SanitizationInterceptor()).toBeDefined();
  });
});
